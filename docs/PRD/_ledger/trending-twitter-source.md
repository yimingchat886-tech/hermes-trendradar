---
concept: 重开 v2.1 热点系统的两个关键设计（热点系统设计 + 表格设计）讨论，并新增推特（X）第一手信息源：① 推特上高热度的 AI 讨论热潮/信息 ② AI 制作者、创作者发布的 AI 教程
slug: trending-twitter-source
date: 2026-07-04
status: prd-generated
open_questions: 1
---

# PRD 审查 Ledger：trending-twitter-source

> 用途：把概念拷问清楚，问答在此留档，防上下文压缩丢失。
> PRD 正文（PRD_MASTER.md / 热点模块 sub PRD）用本文件的 Q-ID 反向引用。

## 概念原文

> 我们重新创建一下关于后续表热点两个关键设计的讨论，后续热点我希望加入一个关于推特的热点寻找。寻找第一手信息源，包含：
> 1. 推特上热度高的 AI 讨论热潮或 AI 信息。
> 2. 一些 AI 制作者、创作者做的 AI 教程。

（追问确认：「两个关键设计」= 热点系统设计 + 表格设计。）

## 四象限审查

### known knowns（已明确，可直接依赖）

- 热点信息系统定位为 **v2.1**，排在 v2.0 真实分析闭环之后（PRD_MASTER §6）。
- 本轮讨论边界 = 两个关键设计：**热点系统设计** 与 **表格设计**（用户已确认）。
- 新增信息源：推特（X）第一手内容，两类目标——① 高热度 AI 讨论热潮/信息；② AI 制作者/创作者发布的 AI 教程。
- v1.3 存量热点设计资产：热点三态（及时/潜在/上升）、S/A/B/C 源分级、表 5 热点雷达表、RSSHub→TrendRadar 链路、突发热点捕捉、跨源事件聚类。
- repo 现实约束：`hermes_benchmark` 零运行时依赖（stdlib only）；hermes-agent 是唯一 LLM 决策层；飞书写入唯一确定性护栏是 CLI 侧 allowlist + fail-closed；**无凭据红线**（no cookies / login state / tokens，redaction 是承重墙）。
- v2.0 已定决策的约束：digest 走消息推送为主；Bitable 写表是条件触发的 M3（消息流运转 ≥2 周后才启动）。

### known unknowns（知道但没定义清楚 → 见问答表）

- 推特数据到底怎么拿到：X 官方 API（付费/限流）、RSSHub 推特路由（需 auth token，与无凭据红线冲突？）、第三方聚合、人工投喂？→ Q1
- 「热度高」如何量化：赞/转/浏览阈值？时间窗口？相对账号体量还是绝对值？
- 「AI 制作者/创作者」名单从哪来：人工 registry（对齐 account_registry 模式）还是自动发现？
- 「教程」类内容如何识别：关键词规则还是 Hermes 判断？
- 表格设计的对象：只指表 5 热点雷达表，还是含新增推特源表？与 v2.0 M3「条件触发写表」的关系？
- 本轮产出的验收方式：设计文档本身如何算「讨论完成」？
- 抓取频率与时效性要求（热点生命周期短 vs 采集成本）。
- 英文推特内容 → 中文选题之间的语言处理跳。

### unknown knowns（隐性假设，需用户确认/否认）

- [x] ~~假设推特第一手数据可以稳定且低成本获取~~ → 已证伪并显式化：选定 RSSHub/Nitter+小号路径，接受不稳，以降级设计+官方 API fallback 对冲（Q1）。
- [x] 凭据边界已裁定：cookie 只存在于 `_external` 的 RSSHub/Nitter 实例 env，本 repo 零凭据不变量不动（Q1）。
- [x] v1.3 架构不整体沿用：推特链路绕过 TrendRadar；TrendRadar 推迟到多源阶段再评估（Q4）。
- [x] 表格不在本轮定到字段级：只定原则，字段以实测为准（Q3）。
- [x] 推文复用现有去重 ledger 模型（platform='twitter'）（Q6）。
- [x] 英文翻译/摘要由 Hermes 分析时包办，采集层不翻译（Q6）。
- [x] 默认每日一次采集，与对标 run 同节奏，不预承诺实时性（Q6）。
- [x] 小号注册/养号/补号由人工运维，repo 只感知源可用性（Q6）。

### unknown unknowns（激发出的盲区）

- **失败边界**：X 反爬/封号/限流；RSSHub 推特路由长期不稳；源失效时热点系统是否可降级运行。
- **合规与红线**：X ToS 对爬取的限制；凭据红线与推特抓取手段的结构性冲突。
- **成本**：X API 付费档位是持续性成本，谁付费、预算上限。
- **数据流断点**：英文内容的翻译/本地化在哪一跳发生（采集层？Hermes 层？）；推文 → 选题建议的证据链。
- **演进方向**：后续还会加 GitHub/HN/公众号等源——推特接入是「一次性专用」还是「源适配器范式的第一个实例」？（反向风险：为此过早抽象通用适配层。）
- **时机风险**：v2.0 尚未开工，现在把 v2.1 设计做细，等真正开工时外部现实（X API 政策、RSSHub 可用性）可能已变，设计会腐烂。

## 8 项审查结论

| # | 审查项 | 状态 | 备注 |
|---|---|---|---|
| 1 | 需求目标 | 清楚 | 每日可追溯的推特 AI 热点 digest（热潮+教程），见 sub PRD Goal |
| 2 | scope / non-goals | 清楚 | 推特单源切片；平台级热度、多源、跨源聚类、字段级表设计均明确不做（Q2/Q3/Q4） |
| 3 | 验收标准 | 清楚 | 7 天连续 digest + 证据链 + degraded 标注（Q5） |
| 4 | 返工模糊点 | 已消解 | 获取路径已定且带 fallback；表字段显式推迟避免返工 |
| 5 | 过度设计 | 已防范 | 不预建通用源适配层（第二个源出现前不抽象）；表只定原则 |
| 6 | known unknowns | 已收敛 | Q1-Q6 answered；Q7（实例选型部署细节）open，不阻塞 PRD |
| 7 | unknown knowns | 已确认 | 8 条假设全部显式化并确认（见上） |
| 8 | unknown unknowns | 已覆盖 | 源不稳→降级 REQ；凭据红线→边界裁定；成本→fallback 有价路径已知；设计腐烂→表字段推迟 |

## 问答表

> 每得到一个答案立即追加一行。未决的 status 记 `open`，并计入 frontmatter open_questions。

| Q-ID | 问题 | 为什么问（影响维度） | 答案 | 由此产生的决策 | status |
|---|---|---|---|---|---|
| Q0 | 「两个关键设计」指什么？ | 文件边界/工作量 | 表格设计 + 热点系统设计 | 本轮 sub PRD 覆盖热点系统设计与表格设计两章，推特源为新增输入 | answered |
| Q1 | 推特第一手数据用哪条路径获取？（调研事实：X 官方 API 2026-02 起 pay-per-use，$0.005/读，无免费层；RSSHub 推特路由需登录态 auth_token，实测频繁 403/空结果；Nitter 2025 复活但公共实例脆弱，自建也需真实账号 token） | 架构/成本/凭据红线 | RSSHub/Nitter + 小号 cookie（用户在被明确告知官方 API 更稳后仍选免费路径） | ① 凭据边界裁定：cookie **只存在于 RSSHub/Nitter 自身部署的 env 中**（对齐 MediaCrawler 外部运行时模式，位于 `_external`），本 repo 只消费其 RSS/JSON 输出，profiles/代码/fixtures 仍零凭据——红线不开口子，只画清边界；② 因源不可靠，热点系统必须**降级可运行**：推特源失效 = 源级 degraded 状态，不阻塞其他源，不算系统故障（进 REQ）；③ 官方 API 记为 fallback 路径（如小号方案被封锁，换路径不改上层设计） | answered |
| Q2 | 目标①「热度高的 AI 讨论热潮」在 RSSHub 路径下怎么实现？（RSSHub 只能按账号订阅，拿不到平台级热度指标） | 架构/工作量 | 账号名单代理热度：人工维护高信号 AI 账号名单，多账号短窗口讨论同一话题 → Hermes 判定热潮 | ① 目标①②合并为**同一条链路**：账号订阅 → 采集 → Hermes 分析（教程=单账号内容类型标签；热潮=跨账号话题收敛）；② 不做平台级热度检测（进 non-goals）；③「热度高」不设数值阈值，由 Hermes 语义判定跨账号收敛，判定证据（哪些账号、哪些推文）必须落到产出物可追溯；④ 推特账号名单复用 account_registry 人工登记模式 | answered |
| Q3 | 表格设计这轮定到什么深度？（v2.0 已定消息流优先、写表条件触发 M3，热点表现在定字段级有过早设计风险） | 工作量/返工风险 | 研究表格设计原则，具体字段边测试边修改 | ① sub PRD 表格章只写**设计原则**（数据先落 SQLite、字段所有权三方归属、表 5 定位与建表触发条件、去重键适配推文），不写字段级 schema；② 字段迭代发生在实施期，PRD 标注「字段以实测为准」；③ 与 v2.0 M3 节奏对齐：热点建表同样条件触发 | answered |
| Q4 | v2.1 首发范围怎么切？（v1.3 蓝图是多源+跨源聚类一整套，v2.0 刚定过价值先行小切片路线） | scope/工作量 | 推特先行，单源切片 | ① v2.1 第一切片 = 推特源闭环（账号订阅→采集→Hermes 判定→digest）；② 热点系统设计以「源接入范式」形式写：单源验证范式，后续源按同范式复制；③ TrendRadar/国内热榜/跨源事件聚类推迟到多源阶段（进 non-goals + roadmap hook）；④ 跨源聚类在单源阶段无法验证，明确不做 | answered |
| Q5 | 推特切片的验收口径？（需可观察、能验证源不稳风险） | 验收 | 7 天连续 digest + 证据链 | 验收标准：① 连续 7 天每日推特 digest 自动推送内部群；② 每条热潮/教程条目含原推链接与判定依据（哪些账号在讨论）；③ 源失效当天 digest 明确标注 degraded 而非静默缺失（同时验证降级设计） | answered |
| Q6 | 四条默认假设批量确认：英文由 Hermes 包办 / 复用去重 ledger / 每日一次采集 / 小号人工运维 | 数据流/工作量 | 四条全部确认 | 分别进 REQ（Hermes 判定含翻译摘要）、REQ（platform='twitter' 走现有 ledger）、REQ（每日采集，不承诺实时）、Out of Scope（账号生命周期管理不入 repo） | answered |
| Q7 | RSSHub vs Nitter 自建选型、实例部署细节（`_external` 内的部署形态、token 轮换方式） | 架构（实施级） | — | 不阻塞 PRD；开工前回本 ledger 补答，sub PRD 标 TODO(Q7) | open |
