# Install v2.0 Extra locally and track releases

## Intent Snapshot

Install the latest published v2.0 Extra release as a stable local trendradar-media command for downstream Agents, create a regular local profile and external run root, and activate a user-level release updater that installs every newly published non-draft release without enabling cleanup or performing a live media download.

## Requirements

- `INSTALL-V2-0-EXTRA-LOCALLY-AND-TRACK-RELEASES-REQ-001` [owner: codex]: Install the latest published v2.0 Extra release as a stable local trendradar-media command for downstream Agents, create a regular local profile and external run root, and activate a user-level release updater that installs every newly published non-draft release without enabling cleanup or performing a live media download.

## Logical Checks

- `trellis.unittest.focused`
- `trellis.diff.check`
